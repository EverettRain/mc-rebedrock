# ReBedrock 更新日志

[English](CHANGELOG_EN.md) | [简体中文](CHANGELOG_CH.md)

这里记录所有面向玩家的重要变更。项目在 Beta 阶段采用简单的版本化历史。

## ReBedrock 26.1beta1

### 修复

- 铁砧的改名框现在会显示物品当前的名字（此前只有改过名的物品才显示，普通物品是一片空白）；
  没有动过框就直接取走结果，不会再被当成一次改名收费。
- 修正无边框输入框里文字偏下偏右的问题——原版的无边框输入框既不内缩也不垂直居中，
  它的外框来自界面美术本身。铁砧改名框与聊天输入框都属于这一类，两处一并修好。

- 修复铁砧界面命名框显示为一条红条的问题（底图那块红色是等待被覆盖的占位标记，界面漏画了那张贴图）。
  该输入框目前是置灰状态：重命名尚未实现。
- 铁砧的价钱涨过 40 级时，现在会像原版一样把那行字换成「过于昂贵！」，而不是继续显示一个已经不可能
  支付的数字。（创造模式不受这道限制，和原版一致。）两件东西合不到一起时，输出槽上的红叉图标现在
  也会出现了。

- 物品提示框此前**根本画不出来**：上一批把画框的代码抽成了一个只会调用自己的空壳，于是三处提示框
  （背包、创造目录、容器界面）悬停时什么都不出现。现已补上真正的框体。
- 提示框不再画出屏幕：贴着右边缘或下边缘悬停时，框会翻到光标另一侧；过长的行现在会自动换行。
- 物品提示框现在会列出附魔。此前提示框只显示名字，附魔完全看不出来——工具附没附上魔、附上了什么，
  都没有任何提示。同时补上了容器界面（箱子、熔炉、工作台、附魔台）**原本完全没有**的物品提示框：
  在这些界面里悬停一件工具，此前连名字都不显示。手持物品在附魔后也会重新弹出名字。
- 修复附魔台三条选项的提示框只有最后一条能看见的问题：提示框画在了逐条循环内部，前两条的提示被
  随后绘制的选项条盖住了。

### 新增

- 铁砧现在可以重命名了。把物品放进左槽、在框里打字，花一级经验即可；把框清空则去掉名字，同样一级。
  改过名的物品在提示框和手持物名里显示新名字，且不会再和没改名的同种物品堆叠。纯粹改名**不会**
  让这件物品下次上铁砧更贵，也不受「过于昂贵」那道墙的限制——被修到极贵的装备仍然改得了名。
  名字最长 50 个字符，支持中文等非 ASCII 字符。

- 输入框现在是真正的输入框了。此前能打字的两处 —— 创建/编辑世界的名称框、聊天与命令行 ——
  各自有一份只做了一半的实现：只能往末尾追加、只能从末尾退格，而且都只收纯 ASCII，
  重音字母和中日韩字符根本进不来（尽管字体本来就画得出）。现在两处共用同一个控件，
  有真正的光标：方向键、Home/End、Shift+方向与 Ctrl+A 选择、Ctrl+C/V/X 剪贴板、Delete、
  Ctrl+方向按词跳、Ctrl+Backspace 删词，以及横向滚动 —— 名字太长不再溢出框外。
  退格删掉的是一整个字符而不是一个字节，多字节文本因此能正确编辑。
  铁砧的重命名框也接到了同一个控件上，但仍保持置灰 —— 重命名还缺一个把自定义名字存到物品上的地方。

- 新增铁砧（三种磨损状态）与铁块。用铁块和铁锭合成，放下后右键打开：左右两个输入槽、一个输出槽。
  可以用同种材料修复工具（每块材料补四分之一耐久，只按真正用掉的材料收费）、把两件同类装备合并
  （耐久相加再加 12%，附魔一并合并：同级升一级、不同级取高的、冲突的那条会被拒绝但仍然收费），
  也可以用附魔书给装备附魔。每次动铁砧都会让这件物品下一次更贵，涨到 40 级就再也修不动了
  （显示「过于昂贵」）。**暂时还不能重命名** —— 那需要给物品存一个自定义名字，是一个单独的存储决策。
- 新增经验修补（Mending）：身上或手里带有经验修补且已受损的装备，会在你捡起经验球时优先吸走经验
  来回耐久（每点经验修 2 点耐久），只有剩下的经验才会进你的等级。

- 物品提示框现在写出这件物品到底有什么：
  - **名字按稀有度上色**，附魔过的物品升一档（普通工具附魔后名字变青色）；
  - **属性行**——武器与工具在主手的攻击伤害与攻击速度，护甲的护甲值与盔甲韧性；
  - **诅咒用红字**列出（绑定诅咒、消失诅咒），与普通附魔一眼可分；
  - 打开 F3 调试屏时另出两行：**耐久**（仅在物品真的有损耗时，剩余/上限）与物品 id。
  提示框里原本跟在名字后面的那个 " x数量" 去掉了——数量本来就画在格子右下角，写了两遍。
  手持物切换时弹出的名字也一并跟上了稀有度颜色。

- 新增附魔台。用四块黑曜石、两颗钻石和一本书合成，放下后右键打开界面：一个物品槽、一个青金石槽
  和三条附魔选项。每条显示等级要求、一句标准银河字母乱码，鼠标悬停还会露出它会附上的其中一个附魔
  以及价钱。买下第 `n` 条要花 `n` 级经验和 `n` 个青金石（条上那个数字是你**必须拥有**的等级，不是
  要付的钱），该条掷出的附魔会全部上到物品上，随后你的附魔种子重掷，下次开出来的三条就不一样了。
  书架能抬高附魔台能给出的档次：与附魔台同高及高一格的两圈 5x5 环内的书架算数，且书架与附魔台
  之间那一格必须是通的——隔着墙的书架一本也不算。关闭界面时两个槽里剩下的东西会还给你。
- 新增附魔书。把一本书放进附魔台，出来的是一本**附魔书**——它是另一种物品，不会和普通书堆叠，
  也不会再被当成合成原料。创造模式的「原材料」页签里每个附魔各有一本，等级都是该附魔的上限。
  （附魔书目前还没有消费它的地方——用它给装备附魔要等铁砧。）
- 新增 11 条游戏规则，它们门控的机制本就已经在，只是一直没有开关：`fall_damage`、`fire_damage`、
  `drowning_damage`、`natural_health_regeneration`（同时门控食物回血与和平难度回血，但不门控饿死）、
  `block_drops`、`mob_drops`（战利品与经验一并）、`spawn_mobs`、`fire_spread_radius_around_player`
  （26.1 用来取代已退休的 `doFireTick`：`-1` 表示任何地方、`0` 表示任何地方都不、正数则是玩家周围的
  方块半径），以及三条命令预算 `max_block_modifications`、`max_command_forks`、
  `max_command_sequence_length`——它们的取值原本就写死在这几个默认值上。
- `/time` 补全为 26.1 的完整形态：`set`（绝对刻，或 `day`/`noon`/`night`/`midnight` 标记）、`add`、
  `pause`、`resume`、`rate`、`query time|gametime`，且每一条都能用 `/time of <clock> …` 指定某一个
  具名时钟。暂停一个时钟只冻结它自己——挖掘、冷却和其它计时照常运行。时间参数支持 `d`/`s`/`t`
  单位后缀（`/time add 2d`）。
- 新增 `/effect give <目标> <效果> [<秒>] [<等级>]` 与 `/effect clear [<目标>] [<效果>]`，玩家与生物通用。
- 新增 `/enchant <目标> <附魔> [<等级>]`，附魔手持物品；超过该附魔自身上限、或物品不适用时拒绝。
- 新增 `/setworldspawn [<坐标>]`，与 `/spawnpoint`（逐玩家）相对的世界出生点。
- 羊现在带 16 种可染颜色：羊毛按羊自身的颜色渲染，用对应染料右键可改色，剪毛后露出无毛
  身体、羊毛层消失；颜色随羊持久保存。
- 新增完整经验系统：等级/进度/总量与 26.1 升级曲线、HUD 经验条与等级数字、经验球实体（重力、
  同值合并、磁力吸附与拾取），以及击杀（受近期玩家伤害门控）、采矿、熔炼与繁育四类来源、死亡
  掉落 `min(7·等级, 100)`（`keep_inventory` 开启时保留）；同时新增 `/experience`（`/xp`）命令。
- 新增下界与末地的世界生成：下界地形（多噪声群系、地表与特征）、末地地形（中心岛 + 外环浮岛），
  各维度带独立世界高度、逐维度独立 tick（未加载的维度零开销）与逐维度分区存档目录。
- 新增更多形状方块：楼梯、门、栅栏门、活板门、按钮、压力板、墙与栅栏——多态形状、方块状态、
  放置与交互，统一走单一 `BlockShape` 的碰撞/射线/剔除路径。
- 台阶与楼梯支持含水（`SubmergedFluid` 状态轴），并激活 Java 互通映射中首条属性偏移覆盖。
- 音频新增 11 类声音分类与各自音量、Directional Audio 开关，以及 biome 环境声循环、洞穴 mood、
  情境音乐与唱片机播放。
- 命令新增 `/execute` 子命令树（as/at/positioned/if/unless/run 等，`run` 重定向回根）、`/help`
  自省与智能用法，以及值类型上下文 Tab 补全；新增 Allow Cheats 世界开关与创建世界页对应勾选。
- 新增游戏版本身份元数据（`26.1` 单一版本源、`version.json` 导出、存档自描述版本头与世界摘要）。
- 自然生成新增世界生成期的动物填充（生成时即在区块内布置畜群）。
- 新增物种清单：新生物现为一行数据，而非一个 C++ 类加一行注册。三个生物借此落地——chicken 与
  sheep（被动）、husk（近战敌对）——按名解析、携带正确的分类与属性、可生成/tick/掉落（chicken 掉
  羽毛）、并按分类遵守 Peaceful 移除规则。与 zombie 一样，它们的渲染资产尚未随附，故游戏内暂不出现。确定性方块掉落（石头→圆石、矿→其物品、玻璃→空等）从挖掘系统的手写
  handler 移入烘焙掉落 floor + 数据包 overlay（`data/<命名空间>/loot_tables/blocks/*.json`）；完全没有
  `data/` 的构建仍靠内置floor掉落，无掉落表的方块掉落自身。掉落结果完全一致。掉落格式**刻意**是方块→
  掉落的直查表，无 chance/Fortune/精准采集机制——本项目无系统让其变化——真正随机的少数方块（树叶/沙砾/
  作物）保留各自的程序化 handler。
- 合成与熔炼配方现从数据产出，不再是合成系统里的静态 vector。内置配方是加载时解析的烘焙
  constexpr floor，数据包可经数据 codec 增改配方（`data/<命名空间>/recipes/*.json`，`type: "smelting"`
  选熔炉形）；完全没有 `data/` 的构建仍靠内置floor合成。求值器不变、配方结果完全一致——配方列表只是
  从求值器搬进数据层；overlay 若引用本 build 没有的物品则整条跳过，绝不解析出错误配方。
- 方块标签的数据包文件格式改由数据 codec 解析：单一 `TagFile` codec（`{replace, values}`，
  `values` 项支持裸字符串或 `{id, required}` 对象）取代 BlockTags 内联手搓的 JSON 遍历。标签策略
  不变——`#tag` 引用照常展开、数据包仍低→高合并且 `replace` 截断、未知方块仍跳过——成员仍存于
  per-id bitset，工具规则行为与之前完全一致。物品/实体标签日后复用同一 codec。
- 新增 Java 版互通的数据侧映射层：一个可行性映射，把 vanilla `minecraft:` 数据包 JSON（配方/掉落/标签）
  摄取成本项目自己的定义。标签直接摄取（vanilla 标签文件即本项目标签格式），合成/熔炼配方经薄适配器，
  方块掉落仅摄取确定性子集（纯掉落，或 silk/非-silk 对的非-silk 分支）；随机或条件掉落标记为需转换。
  完整性检查确认每个方块与物品都映射到 `minecraft:` 名。这是世界互通的数据半边——存档/世界半边
  （block-state palette、scheduled tick、NBT）另计——且为带测试的能力，尚未接入加载；见 wiki 互通报告。
- 新增数据 codec 与双层烘焙基建（`src/data/`），用于把配方、掉落、标签从手抄进 C++ 外置成
  文件。serde 式 `Codec<T>` 以值语义在 POD 与 JSON 间双向编解码（不用函数式管道）；
  `DataStore<Def>` 在底铺一层烘焙的 constexpr 内置floor（落 `.rodata`、零启动解析），
  数据包 overlay 按名合并在上，而完全没有 `data/` 的构建仍靠floor可玩。离线烘焙生成器
  （`tools/bake`）经同一 codec 把源 JSON 转成 constexpr floor；`core::Json` 新增 `dump()`
  序列化与解析计数。此为配方/掉落/标签所依赖的基建，尚未迁移任何面向玩家的内容。
- 新增不依赖 GLFW 或 Vulkan 的权威 `GameRuntime`，统一拥有世界、存档仓库、游戏会话、
  模拟线程、世界锁和服务端命令树。世界创建/加载/保存/卸载与 20 TPS Tick 不再由
  Vulkan 客户端保存；无头测试可运行完整游戏链，并可统计服务端区块状态与光照的驻留
  内存，为后续客户端/服务端进程分离建立明确边界。
- 玩家输入新增线程安全的语义命令队列，破坏、攻击、方块/物品使用、快捷栏切换、槽位
  点击和聊天均以 `GameCommand` 入队，由 `PlayerInteraction` 在权威 Tick 内按顺序执行。
  `PlayerActionState` 同时提供挥手和持续使用的 Tick 时间线，渲染器只读取结果快照。
- 玩家控制器、背包、生命状态、合成、游戏模式、输入与动作时间线现统一归入按稳定
  `PlayerId` 索引的权威 `ServerPlayer`；当前本地玩家也通过同一容器访问，为后续接入多个
  客户端保留明确的数据边界。伤害、死亡、复活、进食、工具耐久与死亡掉落等玩家作用域
  操作均按 `PlayerId` 执行，双玩家隔离测试会验证其中一人的状态变化不会影响另一人。
- 自然生成改用按生物群系和生物类别划分的 26.1 风格加权生成表，包含猪、牛与僵尸的
  内置原版权重和群组大小；资源包可通过
  `data/minecraft/worldgen/biome/<生物群系>.json` 覆盖单个生物群系，未提供或损坏的
  数据仍安全回退到内置默认。
- 实体类型新增 `ON_GROUND`、`IN_WATER`、`IN_LAVA` 与 `NO_RESTRICTIONS` 生成放置规则。
  生成器会在区块列的实际高度范围内抽取位置，并检查承重面、两格身体空间、流体、
  完整实体包围盒与三维玩家安全距离，为洞穴和未来的水生/熔岩生物提供正确路径。
- 世界高度抬到 384 格（y −64..319，24 个 section），对齐现代高度标准：世界访问、
  光照、网格化、流送、存档与玩法各层都带上 `kMinY` 偏移，地形生成器保持 0..255
  噪声纵轴、下方填充深部石头。
- Chunk 所有权拆分出权威服务端世界与渲染端客户端区块缓存：渲染端网格/光照/采样走
  自己的世界副本，每条流送批次与模拟编辑同时落到两侧，各自将来可以丢掉对方要的
  那一半。两侧各自计量区块内存（resident-bytes 预算，冒烟世界里各约 54 MB）。
- 存档改为 region 化。区块编辑与生物不再挤在单个 `world.dat` 里：每个 32×32 区块
  region 有自己的 `region/r.<rx>.<rz>.cache` 文件，一个 chunk 连同它的编辑与它的
  生物一起落盘。区块离开模拟半径时会把编辑与生物提交给后台持久化队列，同一批
  region 写入会合并，并在切换世界或退出前等待落盘；生物随区块离开模拟并在重新流入
  时恢复。玩家放下的方块与加载范围外的生物都不会因崩溃或存档丢失，旧存档在下次
  保存时迁移到 region 布局。
- `GameCommand`、玩法事件与玩家/世界快照新增统一的带长度二进制帧编解码，物品、方块
  状态与生物类型均以注册表身份传输；未知消息可按长度跳过，截断或未知内容会安全
  拒绝。命令、事件和服务端→客户端镜像由此具备可直接接入 loopback/TCP 传输的协议边界。
- 新增传输层（`src/net/`）：命令、快照与事件共用一条消息流经 `MessageChannel` 收发，
  由共享二进制帧编码并按 tag 路由，并提供仿原版本地连接的进程内 loopback 通道对。
  headless 测试用真实游戏会话跑通「命令 → tick → 快照」经字节通道的完整往返——为单人
  也走联机客户端同一条客户端/服务端消息路径打好地基。
- 新增橡木、云杉、白桦、丛林、金合欢、深色橡木、石头、圆石、石砖与平滑石头半砖。
  半砖具备 bottom/top/double 方块状态、半高网格与碰撞/射线轮廓，可按点击面放置、同类
  合并为双层，并完整支持存档、掉落数量、创造目录和半高 HUD 图标。
- 新增 POSIX TCP `MessageChannel` 与监听器：独立 I/O 线程会重组 TCP 的半包/粘包，保持
  帧顺序，以 8 MB 有界发送队列施加背压，并安全处理断线和异常长度；同一套命令→Tick→
  快照链由此可不改上层协议地从进程内 loopback 切换到真实 socket。
- 新连接新增协议握手：客户端和服务端在进入玩法消息前交换应用标识与协议版本，匹配后
  才进入 play；错误应用、损坏 hello、版本不兼容和无响应连接均得到明确拒绝或超时结果。
- 新增不链接 GLFW/Vulkan 的实验性 `mc_rebedrock_dedicated_server`。它可创建或加载世界、
  准备出生区块、以 20 TPS 无头运行并保存，也可在本机 TCP 端口接受一个完成握手的客户端；
  目前提供单连接骨架，图形客户端远程连接入口和多玩家所有权仍属后续工作。
- 新增可选的慢帧与驻留内存诊断：`MC_REBEDROCK_FRAME_TRACE=1` 会分项报告持久化、锁、
  GPU fence、事件排空等超时阶段，`MC_REBEDROCK_MEMORY_REPORT=1` 会统计服务端/客户端
  区块、工作区块、纹理、GPU 缓冲与 CPU 网格池，方便定位双世界架构的时间与内存回归。
- 在 `src/core` 搭建运行期内容身份底座：强类型稠密 `uint16` id
  （`BlockId`/`ItemId`/`EntityTypeId`/`BlockEntityTypeId`）、`Identifier` 驻留器，以及带
  `Bootstrap → External → Freeze` 生命周期的通用 `Registry<Def, Id>`——内置 id 稳定、外部
  （mod/数据包）内容随后注册、错相位/重名/非法 id 访问一律 abort。方块表被灌入一个冻结的
  注册表并与现有 `Block` 枚举并行，暂无行为变化，为后续淘汰 256 顶枚举及其 switch 铺路。
- 方块身份相关的表改按 `BlockId` 索引、按方块注册表定容，不再受 256 顶 `Block` 枚举限制，内容可
  突破旧上限：interned 状态元数据以 `BlockId` 记录每个状态的方块、方块 tag 表与存档块调色板按
  `blockCount()` 定容（调色板彻底去掉编译期 256 顶）、创造栏目录降为身份来自注册表的 view。`Block`
  枚举保留为同一 id 之上的过渡 handle，故无行为变化、存档格式不变。淘汰剩余 `switch(block)` 留待后续。
- 方块 tag 成员关系改为按需跨多个 64 位 word 的 `TagBitset`，tag 种类可突破原先 64 个上限而
  热路径仍只需一次索引与按位与。方块注册表也会在内置内容完成后进入 `External` 阶段，再冻结；
  外部 mod/数据包方块可以顺延取得 id，同时不改变任何内置方块的稳定 id。

### 变更

- 移除降雨的「粒子雨」档，雨模式现在只有「贴图雨」与「异步粒子雨」两档。被移除的那档与异步
  粒子雨用的是同一批雨滴、产出同一份视觉，区别只在于它逐雨滴发一次绘制调用——那是当初为了和
  异步路径做直接性能对照才临时留下的绘制方式，在最高粒子档满雨时意味着每帧一万八千次绘制调用。
  旧存档里选中该档的设置会自动迁移到异步粒子雨。

- 游戏规则名改为 26.1 的 `snake_case` 写法：`doDaylightCycle` → `advance_time`、
  `doWeatherCycle` → `advance_weather`，`keepInventory`、`randomTickSpeed`、`sendCommandFeedback`
  则改为 `keep_inventory`、`random_tick_speed`、`send_command_feedback`。旧版本存的世界不会丢设置：
  读档时仍认得旧名，不会有规则悄悄回落到默认值。`/gamerule` 与补全只接受新名。
- `/time set <n>` 改为按 26.1 的语义设置绝对时间，不再把数值折进当天。`/time set day`（及其余标记）
  仍然是前移到该时刻的下一次出现，因此不会把日历倒回去。
- 玩家动画迁移到骨骼遮罩 + 覆盖混合 + 动画控制器的分层栈，世界玩家与第一人称统一走该栈，
  并按 26.1 规格校准各动作剪辑。
- 存档目录改按世界名 slug 命名，不再追加创建时间戳串；仅在重名时追加数字后缀。
- 实体类型现与方块、物品共用同一套内容注册表：本 build 不再认识的生物——被移除的数据包/mod
  放置过的——不再从存档凭空消失，而是保留为占位，按名 round-trip，等内容重新装回即恢复为真实
  生物。名字经 `rebedrock:` id 旁的 `minecraft:` 别名解析，没注册过的名字干净落空（故命令无法召唤
  出占位）。
- 实体属性现从数据产出。每个生物的生命、移动速度、攻击、跟随范围、击退抗性是一张定长数组，带
  编译进构建的内置floor，数据包可经 `data/<命名空间>/entity_attributes/<物种>.json` 按生物逐属性
  覆盖；未列出的属性回退到floor，完全没有 `data/` 的构建保留每个生物的内置数值。由于 AI 经同一
  路径读跟随范围与攻击，数据包把某生物跟随范围调小即改变其索敌半径，无需改一行代码。
- Vulkan 客户端改为组合 `GameRuntime` 并委托世界生命周期、存档、模拟与权威命令，
  移除其内部对应实现；客户端只保留窗口、输入采样、菜单、声音和 GPU 表现职责。
- 第一人称破坏、放置与进食动画不再按渲染帧自行计时，而是从玩法 Tick 的动作进度纯
  采样；同一操作在不同帧率下消耗相同 Tick，交互判定与可见动作保持同步。
- 玩家渲染改为读取玩法线程每 Tick 原子发布的 `PlayerTickSnapshot`，并用当前帧的部分
  Tick 统一插值位置、步幅、速度、视野、挥手和物品使用进度；HUD、第三人称模型与挖掘
  覆盖也从同一快照取得生命、模式、手持物品、水中和落地状态，不再读取可能正被模拟修改
  的实时玩家对象。连续挥手通过动作序列识别重启，避免手臂从中途倒放回起点。
- 新增每 Tick 原子发布的 `WorldSnapshot`，统一向客户端提供时间、命名时钟、天气渐变、
  相关游戏规则和箱盖状态；天空、阴影、降雨和箱子绘制不再直接读取实时玩法系统。
- 玩家、世界与实体渲染快照新增驻留内存统计与预算回归测试；动态缓冲在 Tick 间保留
  容量并重复使用，避免双世界架构通过逐 Tick 重分配重新引入不受控的客户端内存开销。
- 容器与创造交互全部命令化，容器显示改读每 Tick 快照：背包、箱子、合成、熔炉与创造目录
  的槽位点击、Shift 快速移动、拖拽分配（QUICK_CRAFT）与双击收集（PICKUP_ALL）均作为
  `GameCommand` 在权威 Tick 内由 `PlayerInteraction` 执行，渲染器不再直接修改实时背包/
  容器状态；拖拽目标以（槽位类型, 索引）值对携带，拖拽预览从发布的容器显示快照读取。
  同一交互在任意帧率下消耗相同 Tick，为客户端/服务端进程分离铺平路径。
- 自然生成按类别逐 Tick 分摊与模拟范围相称的区块列采样，按整数权重选择物种，取代
  每秒集中执行三次的表面扫描；被照亮的地面继续以方块光阻止怪物生成，不再随夜色被
  错误调暗。
- 渲染线程不再整帧持有共享世界锁。玩家、世界与实体快照作为一个不可变快照包原子发布，
  读取者在复制期间持有该包，写入端只复用已无读者的缓冲，避免双缓冲在慢帧下被提前覆写。
  渲染端只读自己的客户端区块缓存与已发布快照，GPU fence 等待、
  提交与 Present 不再持任何锁——模拟线程不再等显卡。世界锁现在只覆盖一次 tick 与短暂
  的安装/编辑写段。这消除了移动时的间歇性掉帧：模拟线程不再被横跨垂直同步的整帧读段阻塞。
- 区块生成、初始光照和网格构建现在顺序复用同一个常驻限宽线程池，不再为每个 24 区块
  批次的每一阶段反复创建线程。流送批次以只读共享区块传递，新地形在工作世界、模拟世界
  与客户端缓存之间共享底层数据，只有实际编辑该区块的一侧才执行整块写时复制，降低移动
  加载时的 CPU 峰值和内存增长。加载半径之外额外保留两圈区块作为卸载滞回，避免玩家在
  边界附近移动时反复卸载、重载和写入 region。
- MSAA 颜色与深度等瞬态 Vulkan attachment 会优先使用 lazy allocation；在支持 memoryless
  attachment 的 Apple GPU 上不再为这些仅在 tile 内存中使用的目标分配约 281 MB 常驻显存。
- 模拟线程产生的声音、粒子、容器打开和进食表现统一通过线程安全事件队列交给客户端；
  容器鼠标命中与拖拽只构建无存储指针的纯布局槽位，不再从渲染线程读取权威背包或方块实体。
- 单人游戏现已实际使用进程内 loopback 双向消息链：所有语义命令先编码后由服务端 Tick
  消费，玩法事件以及玩家、世界、实体快照则反向传入 `ClientMirror`，HUD 与世界渲染不再
  直接读取权威 `GameSession`。切换世界会清空通道和镜像，挖掘/容器状态随快照传输，并以
  每 Tick 编码大小回归测试约束全量镜像开销。
- 连续移动、视角、跳跃/疾跑边沿与自动跳跃现作为 `MovementInput` 经消息通道发送，复活和
  模式切换则使用独立的 `SessionCommand`；客户端不再写入或直接调用权威 `GameSession`，
  飞行与疾跑许可也由服务端按游戏模式和饥饿值重新推导，而不信任客户端结果。
- 方块基础形状统一由 `BlockShape` 提供，射线命中、选择框、玩家/生物碰撞与放置占用不再
  各自维护方块特判。完整方块和半砖走连续高度列，火把、植物与箱子走精确 AABB 集合；
  箱子的碰撞现在与 14×14×14 像素模型一致，为后续楼梯、栅栏和门的多盒形状预留同一路径。
- 方块状态网络编码改为遍历 schema 中声明的全部属性并传输「属性索引+值」，取代只覆盖
  六种属性的手写位标记；未知的新属性仍可按项跳过。由于线上布局不兼容，该阶段曾将
  握手协议版本升到 3。
- 方块身份在线上从约 20 字节的标识符字符串改为 2 字节稠密 `BlockId`。握手会按服务端 id
  顺序发送方块注册表名称快照，客户端按名称建立 peer-id→local-id 重映射，因此双方即使以
  不同顺序注册外部内容也不会把方块解释错；共享注册表的单人 loopback 保持零重映射开销。
  这项后续协议变更将当前握手版本进一步升到 4。
- 手持与世界掉落的半砖改用半高方块模型和正确的侧面半幅 UV，不再显示为完整立方体或
  直立的平面物品；水平侧面放置和合并半砖会使用射线的精确命中高度选择 top/bottom。
- README 已按当前 Beta 实现重写，更新 Java 26.1 资源包要求、双平台构建/测试流程、
  运行目录、项目结构与已知边界，并移除过时的 1.16.1 资源提取和内嵌说明。
- 存档格式 18 的迁移已用真实旧世界（格式 10 / 14 / 15）验证通过：每个旧世界都能
  打开、保存时转换为当前格式并重载，玩家位置、背包、方块编辑、天气、时钟与实体
  全部保留。新增 `migration_diag` 小工具，可在任意世界目录的副本上重放「加载 →
  保存 → 重载」往返，随时复核任意真实存档的转换。

### 修复

- 修复观察者这类六面朝向方块的**掉落物**被画成扁平的 2.5D 贴图：同一个方块拿在手里、放进背包
  都是立方体，掉在地上却是一张贴图。「这个物品要不要按立方体画」原先在三条物品渲染路径
  （掉落物、第一人称手持、背包图标）里各手写了一份判断，三份口径并不一致——引入六面朝向方块
  时只补了后两处。三处现已共用同一个判断，任何新增的方块模型只需在一处表态。

- `advance_time` 不再覆盖 `/time pause` 暂停的时钟。该规则是所有时钟之上的全局开关，与每个时钟自身的
  暂停状态相互独立，与 26.1 一致。
- 修复动物模型的 Box-UV 纹理映射，使牛、猪、羊、鸡逐面对齐 Java 26.1：修正了立方体前/后
  面的左右镜像、左右对称肢体内外侧纹理被交换，以及旋转躯干（牛/猪/羊/鸡躯干、牛乳房、羊毛
  体）因骨骼旋转符号错误导致的前后错乱与错位。羊现在正确显示白色羊毛——羊毛部件采样独立的
  羊毛纹理层，不再套一层无毛身体，头部也不再把脸糊到多个面上。
- 声音距离衰减修复：此前默认 inverse 模型在最大距离外仍停在约 0.1 音量，导致地表能清晰听到
  任意深度的怪物；现改为线性衰减在最大距离平滑归零并剔除超距声源，同时修复多处运行期音频缺陷。
- 修复转视角时中心区块被「饿死」、区块交付乱序与首帧网格化指标失真等区块流送问题。
- 修复动画方块图集被压扁（岩浆块）导致的贴图错位。
- 命令补全不再在输入未以 `/` 开头时错误弹出。
- 由本端已不具备的内容（被移除的 mod 或数据包）放置的方块，现在原样保留在存档里，
  而非被替换成空气。方块的标识符与状态原样留存，重新装回内容即可让方块原地恢复——
  与 Java 保留未知方块（而非丢弃）的做法一致。
- 树苗成长生成的原木现在以完整方块状态原子写入并发布，树干不再因渲染增量遗漏后续
  朝向修改而横向显示端面；跨区块树冠也保留完整状态。
- 玩家背包重新支持按住 Shift 在快捷栏与主背包间快速移动整组物品；创造模式物品
  目录中的快捷栏物品可用 Shift 点击快速丢回目录（清除该格）。
- 怪物不再只能在夜间地表生成而漏掉黑暗洞穴；被火把照亮的洞穴保持安全。猪和牛也
  不再以相同概率出现在沙漠、海洋等不属于其 26.1 生成表的生物群系。
- 树叶过滤后的天空光不会再让下方草方块在夜间错误退化为泥土；草的存活只取决于上方
  方块的物理遮挡，向附近泥土传播仍按当前亮度限制。
- 完全退出游戏后重新进入世界，玩家不再被传送到世界原点 (0,0,0)：加载流程此前用尚未
  初始化的渲染快照覆盖了正确恢复的存档坐标（热重进正常只是残留状态的假象）。世界加载
  后、首个模拟 tick 前，玩家位置与渲染快照现在都与存档坐标一致；切换世界也不会再继承
  上一个世界的位置。
- 玩家、挥动手臂、掉落物、下落方块和生物在 Tick 边界不再偶发抖动：插值比例现在以
  客户端收到镜像快照的时间计算，并与同一次镜像更新中的前后端点配对，不会再错开一个 Tick。
- 渲染帧快于服务端 Tick 时，后续无按键帧不再覆盖尚未消费的跳跃或双击前进边沿；多个
  `MovementInput` 会在服务端合并一次性边沿，双击前进疾跑因而不会偶发失效。
- 半砖的射线命中与选择框不再互相矛盾：瞄准空着的半格不会命中整格空气墙，瞄准实际
  半砖表面则返回正确距离与面；火把、植物、作物和箱子也使用各自真实轮廓。
- top/double 半砖经世界编辑事件同步到客户端后不再退回默认 bottom 状态；此前传输编码
  漏掉 `SlabType`，导致权威世界正确而客户端网格错误。
- 玩家疾跑跨上半砖或其它可跨越高度时不再瞬间停顿或退出疾跑：成功 step-up 会保留水平
  速度并清除已被完整跨越的碰撞标志，真正的一格高墙仍会正常阻断疾跑。
- 压力板踩上去不再把玩家连续顶起弹跳：压力板改为无碰撞（对齐 26.1 `BasePressurePlateBlock#
  getCollisionShape` 返回空），只保留供轮廓/拾取用的薄盒，触发检测改用玩家脚所在的格——消除了
  「有碰撞 + 按压时高度切换 + 探测深度」三者耦合出的振荡。
- 创造模式在物品分类页签下，即使生存背包已满，也能用 Shift 点击快捷栏物品把它快速删除（丢回
  无限目录）：此前当前创造页签是纯客户端状态、从不传给服务端，服务端恒按「物品栏页签」处理，
  删除分支形同虚设而退化成背包已满时的静默失败；现在点击会携带其页签上下文。
- 大量方块的挖掘/放置/踩踏/敲击音效对齐 26.1：方块现拥有完整的声音组（SoundType，从 26.1 源
  逐块转写、作为烘焙属性一次解析成事件 id），退休了此前只覆盖 7 组、把泥土归为草、羊毛只认 3
  色、下界岩/菌岩/玄武岩/灵魂沙等一律回落石头的手写白名单。
- 门、活板门、栅栏门（开/关）与拉杆、按钮、压力板（触发）现在会发出对应交互音效；并补上了此前
  根本无法用右键扳动的拉杆交互。打火石点火发出 `item.flintandsteel.use`（不再误用火焰方块的放置
  音），剪羊毛发出 `entity.sheep.shear`。
- 红石火把全面对齐普通火把：可像火把一样斜挂在墙面放置并按墙面样式渲染，破坏后掉落红石火把
  （不再只能插地）；LIT 状态会切换点亮/熄灭贴图（`redstone_torch`/`redstone_torch_off`），自发光取自
  方块状态（点亮 7、熄灭 0，以区别于普通火把的 14），放置时默认点亮。同时修复红石墙火把挂墙后
  失去碰撞/选择盒而无法拆除的问题——其形状此前按方块身份判定，红石变体落到了错误位置的地面盒，
  拾取射线因此打不中；现统一按「是否墙面火把」的特性决定形状与网格，二者不再各行其是。

## ReBedrock beta5

### 新增

- ReBedrock 自有界面文本新增独立的 `rebedrock` 语言命名空间，并沿用 Java 26.1 的英语基础层、当前语言层和资源包覆盖顺序。客户端内置英文与简体中文选项翻译，资源包可通过 `assets/rebedrock/lang/<语言代码>.json` 补充任意语言，无需修改原版语言文件。
- 现在可以在运行时使用标准 Java 资源包。ReBedrock 会发现 `resourcepacks/` 下的目录和 `.zip`，按文件名确定覆盖顺序，并仅在资源实际使用时将压缩包条目解压到 `.packcache`。纹理、音效、字体和翻译均通过统一的命名空间资源提供器解析。
- 音效播放改由当前资源包的 `sounds.json` 注册表驱动，支持追加/替换、权重、单条目音量与音高，以及递归音效事件引用。
- 支持 26.1 字体提供器栈，包括 `bitmap`、`space`、递归 `reference` 和 `unihex`；语言从 `pack.mcmeta` 发现并异步切换。
- 熔炉成为按世界坐标独立存在的方块实体，各自保存输入、燃料、输出和烧炼进度，关闭界面后仍会工作，并随存档持久化。
- 世界时间拆分为始终推进的服务器 Tick 与可独立暂停、调速的命名时钟；太阳、月亮、自然生成和玩法计时不再错误共用同一时钟。
- 新增统一世界变更服务，以 26.1 风格的标志和原因记录统一处理方块实体生命周期、掉落、邻居通知、重新光照和区段失效。
- 玩家破坏与放置、水桶与岩浆桶、锄地、熔炉点火、耕地踩踏、树木生长和模拟方块写入全部改走统一世界变更服务。放置箱子/熔炉会自动创建方块实体、破坏会自动销毁并撒落内容物，不再依赖各调用点自行特判；岩浆桶倒出的岩浆现在会立刻开始流动。
- zip 资源包中的贴图、JSON 与字体现在直接从压缩包内存读取，不再解压到磁盘缓存。
- 修复 zip 形式的数据包（tags 等）此前完全无法解析的问题。
- 潜行并手持物品时，方块可以贴放到箱子和熔炉上了；此前容器界面会无条件抢先打开。
- 创造模式舀水不再把空桶换成水桶。该保护改在游戏模式层统一处理，对所有物品生效。
- 刷怪蛋不再因为生物模型尚未加载完而静默失效。
- 掉落物与下落中的方块现在会随世界保存：此前重新载入后，玩家扔出或挖出但尚未拾取的一切都会消失，塌落中的沙土也会凭空不见。
- 20 TPS 模拟移出渲染线程，运行在独立线程上：单个 tick 变慢不再卡住渲染，渲染与模拟通过插值解耦。设置 `MC_REBEDROCK_SYNC_TICK=1` 可回到同步模式。
- 共享世界改由读写段保护：模拟 tick、玩家交互与区块流送批次各自持写段，每帧的特效采样与绘制持读段。
- 生物、掉落物与下落方块均改由每 tick 发布的渲染快照绘制，绘制过程不再直接遍历模拟正在改动的实体列表。
- 模拟产生的副作用（方块变更、声音、粒子、玩家死亡）改为入队，由主线程每帧统一回放，模拟本身不再在发布事件时触碰渲染状态。
- 方块的定时更新（下落、流体、支撑检查、树叶衰减、树木生长）由五个全局队列收敛为按区块存放的统一调度器：区块卸载时其待办工作随之丢弃，不再对已卸载的格子触发。处理顺序与预算保持不变。
- 随机刻分发由中央 switch 改为按方块索引的函数指针表，抽签循环先做一次预筛再进入调用；优化构建下比原先略快，行为完全一致。
- 方块的可挖掘工具与所需工具等级改由 26.1 的方块 tag 数据驱动（`mineable/*`、`needs_*_tool`），存为每方块一个 64 位掩码，取代原先五条手写 switch。资源包若提供 `data/` 半边可逐 tag 覆盖，否则使用与 26.1 一致的内置默认。
- 方块变更、声音、粒子与玩家死亡收敛为四类事件，经单线程同步分发器投递；`SimulationHost` 保留，由内置订阅者调用。事件负载均为可平凡复制的 POD，为后续模拟线程的跨线程队列留好接口。
- 26.1 的 GUI 精灵缩放元数据（`nine_slice` / `tile` / `stretch`，含逐边边框与 `stretch_inner`）现已解析并生效：菜单按钮、滑块槽与滑块把手改用九宫格绘制，在任意宽度下边框都保持锐利。

### 移除

- 发行包不再包含 `resources/entity`。其中唯一的僵尸皮肤由 Mojang 原版贴图转换而来，与"不分发任何原版资源"的目标相抵触。实体贴图现在只有两个来源：标准资源包，或程序生成的占位皮肤。

### 变更

- 方块状态改由每个方块自己声明的属性列表组合而成（作物的 `age`、耕地的 `moisture`、
  树叶的 `persistent`、熔炉的 `lit`、水的 `level`、朝向），取代原先固定的三条轴。
  存档格式升到 18：方块编辑改为按「方块标识符 + 具名属性」保存，因此以后新增方块属性
  不会再改变文件布局。旧世界照常打开并在下次保存时自动升级；同一批测试数据下，
  存档体积减少约 17%、载入耗时约为原先的一半。
- 将五条高频机制改为适合 C++ 的连续数据路径：音效事件与引用在资源加载时编译为整数索引并缓存静态解码资源；方块状态属性按 raw ID 直接查平表；光照传播复用压缩节点队列和开放寻址去重表；实体仅在跨区段时更新空间索引；寻路复用连续堆与节点表。这些改动不改变存档 ID 或玩法结果，并显著降低大量生物与频繁方块更新时的分配和哈希开销。
- 物品名称改用 Java 26.1 的注册表派生 description ID：普通物品解析为
  `item.<命名空间>.<路径>`，方块物品解析为 `block.<命名空间>.<路径>`，并移除
  C++ 注册表中原有的中英文名称字段。快捷栏、背包提示、命令补全和窗口标题均通过
  当前资源包语言显示；C++ 内部则使用 constexpr 结构化 ID 与无分配异构哈希查询，
  避免在渲染热路径中逐帧拼接 Java 风格字符串。
- ReBedrock 不再暂存或安装 Mojang 的纹理、音效、字体和翻译；启动时必须提供含 `pack.mcmeta` 与 `assets/` 的标准资源包，否则会创建目录并显示双语诊断后退出。
- 渲染与菜单改用 26.1 资源布局，包括命名 HUD/控件/熔炉精灵、菜单图块、独立月相纹理、最终版刷怪蛋图标和温带牛猪皮肤。
- 方块改用驻留化 `BlockState` ID，将方块身份与属性分离；熔炉 `LIT`、墙上火把 `FACING` 等状态可完整保存和迁移。
- 区块区段使用逐区段调色板和位压缩索引保存状态；全空气区段不分配状态堆，流送、网格、射线和光照均传递完整状态。
- 项目文档、实体数据、资源路径和原版行为参考由 Java 1.16.1 更新到 Java 26.1。

### 修复

- 砂岩、红砖、石英块、下界岩与熔炉现在和其它石料一样需要镐才能保住掉落。此前空手也能获得它们，与 26.1 的 `requiresCorrectToolForDrops` 不符。
- 选项字段和值不再混用硬编码中英文。原版字段使用 26.1 翻译键，ReBedrock 自定义字段使用带统一英文回退的项目键；动态内容通过 `options.generic_value` 和 `options.percent_value` 组合，使各语言自行决定语序、标点和百分号格式。
- Java 26.1 生物移动属性现在只在移动积分边界统一换算为 ReBedrock 的内部每 Tick 速度，不再被直接当作内部速度使用；普通移动恢复正常节奏，恐慌目标仍会在其上叠加各物种的受击加速倍率。
- 语言条目采用原版风格的居中 270 像素选择宽度与一致的点击区域；深色列表背景继续延伸到屏幕两侧，滚动条则移动到内缩条目右侧，不再贴着窗口边缘。
- 陆地生物不再把树叶视为自然生成地面或寻路节点。生成与寻路共用同一套承重方块规则，避免生物偏向树冠生成并把树顶当成普通地形。
- 背包打开时按 Escape 会先关闭背包并直接回到游戏，不再让同一次按键穿透到底层游戏页面并打开暂停菜单。
- 牛、猪和僵尸恢复使用 Java 26.1 的真实基础移动速度；恐慌状态的速度倍率不再乘在缩小五倍的旧数值上，逃跑提速重新清晰可见。
- 地形植被移除了非原版的低概率“展示树苗”，树苗只会通过玩家行为出现，不再随区块自然生成。
- 语言列表的滚动条滑块和轨道支持鼠标按住拖动，光标沿轨道移动时会连续更新列表位置。
- 跨区块树冠现在会同时同步玩法方块状态、网格与光照；边界树叶会保留供相邻区块重建，同时存档和运行时编辑保持最高优先级，修复树叶看似完整却实际被截断、交互后短暂正确又在离远后复现的问题。
- 走路脚步声恢复使用原版距离累计公式，普通行走的播放节奏重新明显慢于疾跑。
- 水和熔岩动画不再依赖失效的硬编码图集层号或昼夜时钟；图集层、帧数及资源包 `.mcmeta` 帧时长通过统一渲染契约传递，关闭 `doDaylightCycle` 后仍会播放。
- 创造模式“材料”分类补充熔岩桶，可放置和收集当前仅有源方块的熔岩，以便在完整熔岩流动模拟完成前验证渲染效果。
- 重力方块在起落时立即更新网格，并在空中使用与地形一致的场景光照，消除静态网格/实体切换时的旧几何、错误着色和纹理闪烁。
- 高速下落的重力方块不再穿过单层地面后消失。碰撞会扫掠每 Tick 经过的所有 Y 层；放置失败时转成掉落物，所属区块未加载时暂停实体。
- 温带牛使用与 64×64 皮肤匹配的完整 Java 26.1 成年模型，补回口鼻、角和乳房，并修正前腿位置与左腿镜像 UV。
- 关闭 `doDaylightCycle` 不再冻结挖掘、持有使用延迟等玩法计时，`/time` 修改也不会卡住交互冷却；界面表现改用帧时间。
- 熔炉点燃和熄灭改为同一方块的状态变化，不再替换方块而丢失方块实体或烧炼进度。
- 缺失或损坏的资源包纹理会显示洋红黑缺失纹理并输出诊断，而不再中止图集创建。
- 水和熔岩动画会读取 `.png.mcmeta` 的显式帧顺序，并对非标准帧数循环或截断，避免资源包导致启动失败。
- 创造模式底部两个标签页会分别采样自己的 26.1 标签精灵，不再共用同一图集切片。

## ReBedrock beta4

### 新增

- 雨雪天气会像 26.1 一样压暗实时天空盒、地平线与天空光，并让日月逐渐隐入云层，同时保持玩法光照等级不变。
- 天气系统支持 `/weather clear`、`/weather rain` 及可选持续时间，具有平滑雨量过渡、`doWeatherCycle` 控制和存档持久化。
- 新增磨制花岗岩、磨制闪长岩、磨制安山岩和光滑石头，以及对应合成或烧炼配方。
- `/spawnpoint [<x> <y> <z>]` 支持当前位置、相对坐标与个人重生点持久化。
- 铁桶可以收集静止水源并倒出水；创造模式不会消耗空桶或水桶，射线会跳过流动水。
- 掉落物具有方块碰撞、向玩家移动的拾取动画、同格合并和落地摩擦。
- 选项菜单新增“实验性内容”页面，提供雨渲染模式与太阳阴影开关，并持久化相关设置。
- 方块碎屑与雨粒子改用 GPU 实例化渲染，将大量逐粒子 Draw Call 合并为单次绘制。
- 雨提供贴图雨、粒子雨和异步粒子雨三条可切换路径；贴图雨支持屋顶裁剪、距离透明度、光照和独立水花采样。
- 新增太阳空间阴影深度预处理，并提供 `MC_REBEDROCK_SHADOW_DEBUG=1` 调试叠层。
- 猪、牛和僵尸按物种播放 26.1 环境、受伤、死亡与脚步音效，并使用原版调度、音量和随机音高。
- 雨量大于零时播放雨声音效；室内会使用更轻的屋顶雨声，雷暴会提高音量。
- “实验性内容”新增粒子效果等级：低、中、高、疯狂，控制雨滴预算、粒子上限和事件生成数量。
- `MC_REBEDROCK_PARTICLE_LEVEL=0..3` 可在无界面环境覆盖粒子等级。
- 雨场范围扩大到约 ±24 格、生成高度提高，并加入地面/屋顶/墙面碰撞缓存、随机生成高度、持久风向及可配置缓存开关。
- 生物拥有跨容器压缩保持稳定的 `uint64` 身份，并通过区块区段空间哈希加速附近查询、射线和拾取磁吸。
- 新增模拟距离：范围外生物暂停 AI、移动、重力和计时，但保持渲染与可选中；远距离非动物类别按规则消失。
- 视频设置新增独立的“模拟距离”滑块，范围 2–12 区块并写入 `render.simulationDistance`。
- 新增 26.1 风格自然生成：按模拟范围、地面、光照、生物群系权重、类别上限、玩家安全半径和难度生成生物群。
- 生物会随 `world.dat` 格式 12 持久化物种、位置、朝向、速度、生命、计时器和随机状态。
- 陆地生物新增路径导航、逃跑目标和游泳目标，并可跨 Tick 持续追踪同一攻击者。
- 敌对生物可以通过共享伤害管线近战攻击玩家，伤害随难度缩放。
- 生物待播放音效携带受伤、死亡、环境或脚步事件类型，由宿主选择各物种对应音效。

### 变更

- Vulkan 客户端拆分为设备初始化、纹理/图集、HUD、世界绘制、共享记录和菜单几何等专职组件，外部行为保持不变。
- 环境天气最多占缩放后粒子池的 75%，为方块碎屑和水花保留容量。
- 草与橡树叶颜色在图集构建时烘焙，减少片元着色器的重复工作。
- 太阳阴影预处理默认关闭，并限制投影区段数量以控制成本。
- 太阳空间阴影深度图正式用于地形，支持偏移与滤波以获得稳定阴影。

### 修复

- 生物选取改为检测精确生命实体包围盒，并按命中距离排序。
- 自然生成会验证生物群每个成员的完整 AABB，避免生成在墙体或彼此内部。
- 受击生物恢复重力、落地与摔落伤害，不再悬空。
- 异步雨后端不再用其他模式五倍的雨滴数量进行错误对比。
- 运行中切换和平难度会立即同步到会话并清除不应存在的敌对生物。
- 从选项菜单打开“实验性内容”不再误触发后方控件。
- 雨水花落在水面时会停在真实水面高度，不再继续下沉。
- 高屋顶下找不到露天重生列的雨滴不再卡死或错误穿过遮挡。
- 新渲染管线创建后会销毁着色器模块，修复资源泄漏。
- 构建会暂存完整原版音效树，资源包中此前遗漏的声音可正常播放。
- 进食期间会持续播放咀嚼循环，并在结束时播放打嗝声。
- 背包方块缩略图采用 26.1 固定面亮度，不再受错误场景光影响。
- 背包人物预览会根据鼠标视线驱动骨骼转向。
- Shift 点击合成结果会一次快速移动整批产物，并正确消耗材料。
- 深色橡树生成正确的 2×2 树干、倾斜和原版树冠。
- 沼泽地形按 26.1 高度生成积水，不再形成异常干燥洼地。
- 方块纹理图集按注册表名称构建，消除固定编号与占位层漂移。
- 生物死亡掉落不再错误受创造模式限制。
- 新世界“正在创建地形”不再因生成与流送互等而死锁。
- 生物战利品在死亡 Tick 立即产生，不再等待尸体计时结束。
- 玩家死亡和重生使用一次性死亡守卫，避免重复死亡界面与重复掉落。
- 大面积水体的区块接缝不再因缺少邻区采样而产生明显边线。
- 世界生成不再把草方块留在浅水底部。
- 太阳和月亮改用派生图集层，不再采样错误纹理。
- 生物群系表面边界改为平滑过渡，不再每四格阶梯式跳变。
- 草方块、高草和树叶使用 26.1 逐生物群系颜色。
- 草与叶片颜色会跨生物群系边界平滑混合。
- 火把不再被渲染为大面积透明、带裂纹的错误纹理。

## ReBedrock beta3

### 新增

- 新增“最高”平滑光照档，实现 26.1 风格逐顶点 AO 与邻角光照加权。
- 视频设置新增垂直同步选项 `render.vsync`，并随配置持久化。
- 新增通用游戏规则注册表，通过编译期表统一声明类型、默认值、命令名和显示名。
- 游戏规则以稀疏、自描述块保存到 `world.dat`，并兼容旧存档默认值。
- 新增 Java 26.1 随机 Tick 模拟，`randomTickSpeed` 驱动草扩散、树苗生长、叶片腐烂和农作物行为。
- 菜单按钮按下时播放原版 `ui.button.click`，滑块拖动不重复触发。
- 背包和容器支持 Shift 点击、左右键拆分、拖拽分配、双击收集等基础 26.1 鼠标手势。
- 燃烧中的熔炉会切换正面纹理并发出正确等级的方块光。
- 新增从 26.1 `CowEntity` 与 `QuadrupedEntityModel` 移植的牛。
- 新增小麦、胡萝卜、土豆、种子、农田、成熟阶段、收获掉落和相关配方组成的耕作系统。
- 新增农田湿度；四格内水源会保持湿润，否则逐步干燥并在无作物时退化。
- 新增农田踩踏：满足摔落距离与概率条件时恢复为泥土。
- 掉落物改为 3D；非方块物品使用带挤出边缘的薄模型旋转显示。
- 农作物选择框会随生长阶段从 2/16 增至完整高度。

### 变更

- 平滑光照改为关闭/最低/最高三档，并对应不同网格烘焙策略。
- 区块网格构建通过每请求 O(1) 快照访问区块与邻区，降低锁竞争。
- 新增控制设置页，集中视角摇晃、自动跳跃、鼠标灵敏度和反转鼠标等选项。
- 新增独立语言页，提供可滚动语言列表、Unicode 字体开关及异步字体重建。
- 切换英文后中文语言项仍保持自己的本地化名称，不再被翻译成英文。
- 农田使用 15/16 高度的网格、碰撞和选择框。
- 农作物使用原版 `crop` 方块状态模型，以交叉薄片显示阶段纹理。

### 修复

- 方块破坏碎屑恢复受重力影响，不再向镜头上方漂浮。
- 旧存档中的湿润农田和成熟作物可再次加载，状态迁移不会误解释方向字段。
- 玩家斜向行走时可再次跨上台阶。
- 台阶修复不再允许玩家靠行走攀上一整格方块。
- 树冠生成会跨区块边界写入，不再被区块边缘平切。
- 深水夜间着色不再像自发光。
- 第一人称手和持有方块会采样玩家眼部环境光。
- 海面恢复更清澈的原版颜色和透明度，不再呈灰色浑浊平面。
- 夜间天空光采用冷色月光色调。
- 网格积压溢出时不再永久丢失区段更新。
- “最高”平滑光照跨方块边界保持连续，不再跳变。
- GPU 遮挡查询结果过期时不再造成区段突然闪现或消失。
- 补齐游戏实际播放但构建包中遗漏的两个音效资源。
- 点击背包或容器槽位移动物品时不再错误播放按钮音效。
- 沙子和沙砾恢复下落；加载区块会正确调度重力检查。
- 随机 Tick 不再因全世界扫描和无界写入阻塞帧。
- 草扩散不会在同一位置反复无效写入或制造过量网格任务。
- 新区域流送降低单帧生成、光照和上传峰值。
- 帧率限制器改进唤醒校正，更准确落在目标帧率。
- 缩短的农田旁边方块会保留侧面，不再被错误剔除。
- 牛模型与 26.1 `CowEntityModel` 的身体、头部和腿部比例一致。
- 牛和猪背部不再错误使用腹部纹理。
- 猪腿部步态恢复原版相位，不再落后半步。

## ReBedrock beta2

### 新增

- 新增遵循 26.1 `CubeMap` 的标题画面全景，支持六面立方体、淡入和背景模糊。
- 生存模式快捷栏上方新增经验条。
- 新增 `config/rebedrock.log` 会话日志，镜像控制台输出并记录启动信息。
- 新增简体中文界面、语言设置和原版风格“强制 Unicode 字体”选项。
- 完整加入生存与创造玩法，包括生命、饥饿、氧气、伤害、死亡、重生和模式差异。
- HUD 使用原版 `icons.png` 精灵绘制生命、饥饿和空气条。
- 新增方块附着规则；火把需要坚固支撑，花和树苗需要合适土壤。
- 玩家生命、食物、饱和度和空气值写入存档。
- 新增统一方块/物品内容注册表及创造分类目录。
- 新增 `rebedrock:*` 命名空间标识符，并接受兼容的 `minecraft:*` 别名。
- 世界存档格式 v6 使用命名空间调色板保存方块与物品，避免枚举顺序变化破坏存档。
- 新增方块战利品表，可按工具、精准采集、时运和随机条件产生不同掉落。
- 补齐丛林、金合欢和深色橡木的原木、木板及树苗。
- 补齐五种材料的剑、斧、锹和锄，并加入耐久、挖掘等级和配方。
- `/give <item|index> [count]` 可通过注册名或兼容索引给予物品。
- 苹果和面包可以进食，恢复饥饿与饱和度，并播放使用动画和音效。
- 生物按 26.1 规则承受超过三格后的摔落伤害。
- 游荡生物会跨上一格高障碍。
- 落地会像原版一样取消主动创造飞行。

### 变更

- 世界加载采用两阶段流程，先建立可玩的出生区域，再在后台继续流送。
- 区块生成并行化，噪声、表面、洞穴和特征可由多个工作线程处理。
- 方块通过各自的 `BlockItem` 子类注册和持有，不再用平行表推断行为。
- 右键交互统一通过 26.1 `Item#useOn` 分派。
- 树叶使用专门的 `LeavesBlockItem`，保持颜色与放置后的持久属性。
- 菜单后的 `options_background` 改用优化过的图块绘制。
- 所有菜单共享同一背景资源，单人游戏列表不再重复上传纹理。
- 泥土和其他带色 GUI 精灵在 sRGB 空间着色，避免颜色过暗。
- 新世界以空背包开始，不再预填调试物品。
- 木制方块遵循 26.1 `mineable/axe` 标签及对应挖掘速度。
- 原木方向按点击面轴向放置，熔炉、箱子等水平朝向玩家。
- 点击可替换方块时直接占用该格，而不是总放到相邻格。
- 花、树苗和火把不再被普通方块放置直接替换；流体仍可冲掉合适装饰物。
- 镐等级按原版限制矿石掉落，铁矿和青金石需要正确等级。
- 创造模式按住攻击键会持续破坏方块。
- 方块破坏粒子按真实表面纹理、颜色和方块形状生成。
- 难度移入每个世界存档，并由创建/选择世界界面管理。

### 修复

- 墙上火把的放置偏移与网格旋转统一使用 `FACING`，不再漂离墙面。
- 标题全景广角畸变不再集中在单个角落。
- 标题全景不再上下颠倒。
- 打开玩家背包时生存生命、食物和空气条保持显示。
- 快捷栏上方物品名只在切换持有物品后短暂显示，不再永久显示或暴露内部 ID。
- 五种斧配方同时支持左右镜像布局。
- 强制 Unicode 字体开关不会再挤掉选项页的“完成”按钮。
- 生物、掉落物、重力方块、箱子和第三人称玩家使用正确深度排序，不再隔墙透出。
- 实体会采样世界天空光与方块光，火把、昼夜和地下环境能正确影响亮度。
- 第一人称进食动画使用 26.1 的平移、旋转和时间曲线。
- 创造模式进食不再消耗食物。
- 实体 box-UV 按几何文件声明的纹理宽高归一化。
- 每立方体 `rotation`、`pivot` 和 `inflate` 会真正参与渲染。
- `tools/texture_editor` 预览与游戏实际 box-UV 组合方式保持一致。
- 草、花、树苗和火把等零硬度方块不再闪一下破坏阶段纹理。
- 持有物品从开始攻击/使用时就播放摆动，不再等操作完成才播放。
- 疾跑跳跃速度从约 8.7 格/秒修正为原版约 7.1 格/秒。
- 失去支撑的火把等附着方块会立即掉落并通知后续邻居。
- F3 方块光读数在站立时也会采样正确位置。
- 沙子落下清空原格时会通知邻居，使上方和相邻重力方块继续坍塌。

## ReBedrock beta1

### 新增

- 新增 Vulkan 世界渲染、区块流送、平滑天空光/方块光和基础体素世界。
- 新增原版风格天空：太阳与八相月亮随 24000 Tick 昼夜循环移动。
- 新增水下指数雾与平滑入水过渡。
- 新增生存/创造世界模式、背包、合成、箱子、熔炉和基础方块交互。
- 提供 macOS 与 Windows 启动程序及自包含 `game` 运行目录。
- F3 调试层显示版本、FPS、坐标、天空光和方块光。
- 新增视频设置、帧率上限、抗锯齿、渲染距离和视角选项。
- 新增命令行隔离方块渲染场景，用于视觉回归。
- 新增统一数据驱动动画系统，读取 Bedrock 风格 `*.geo.json`、`*.animation.json` 和 Molang 曲线。

### 变更

- 增量异步光照和区块重新网格显著降低编辑方块造成的卡顿。
- 第一人称持有物品、背包玩家预览、箱盖和生物共享统一动画基础设施。
- F5 在第一人称、第三人称背后和第三人称正面之间循环。
- 第三人称玩家潜行时会下蹲，动画状态随移动、攻击和使用切换。
- 方块选择框追踪火把、植物和箱子等真实形状。
- 第一人称手和持有物品改用世界空间 3D 渲染。
- 有方向方块在存档中保留放置方向。
- 菜单与加载界面使用统一的 Minecraft 风格泥土背景。
- 从上方观察水面时，透明度会随水深明显变化。

### 修复

- 箱盖打开时不再改变尺寸。
- 箱盖会沿箱子自身朝向打开。
- 箱盖围绕铰链刚性旋转，不再横向滑动。
- 箱子顶面与底面纹理恢复正确方向。
- 修复玩家皮肤 UV、箱子持久化与背包掉落、火把光照等首批问题。
- 修复水平原木模型方向、熔炉背包预览和箱子交互等问题。
